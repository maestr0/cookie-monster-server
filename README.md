Cookie Monster server for Raspberry Pi
======================================

installation

  `npm install`
  `npm install -only=dev`
  `touch .env`


  SLACK_API_TOKEN=
  LOGGLY_API_TOKEN=
  LOGGLY_API_SUBDOMAIN=

  
run


  `gulp`
